# Figure 8: independent runs

The `figure` image/PDF is the manuscript figure; `data/` contains its numerical inputs. Existing simulation pipelines remain in their original locations.

Run `python3 figures/08_independent_runs/plot.py` from the repository root (requires Matplotlib). This redraws the figure from the local CSV files.
