# Figure 5: mass scan

The `figure` image/PDF is the manuscript figure; `data/` contains its numerical inputs. Existing simulation pipelines remain in their original locations.

Run `python3 figures/05_mass_scan/plot.py` from the repository root (requires Matplotlib). This redraws the figure from the local CSV files.
